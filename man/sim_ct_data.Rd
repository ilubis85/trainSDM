% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_ct_data.R
\name{sim_ct_data}
\alias{sim_ct_data}
\title{Generate simulated camera trap events based on ecological rules}
\usage{
sim_ct_data(
  cam_points,
  dem,
  canopy,
  river_dist,
  n_days = 30,
  rules = data.frame(species = c("Harimau", "Gajah", "Rusa", "Babi hutan"), elev_max =
    c(2000, 800, 1500, 1000), canopy_min = c(20, 10, 15, 5), river_maxdist = c(2000,
    1000, 1500, 800), prob_camera = c(0.6, 0.8, 0.7, 0.9), prob_daily = c(0.3, 0.4, 0.5,
    0.6))
)
}
\arguments{
\item{cam_points}{sf POINT data. Titik lokasi kamera trap.}

\item{dem}{SpatRaster elevasi (meter dpl)}

\item{canopy}{SpatRaster tinggi kanopi (meter)}

\item{river_dist}{SpatRaster jarak ke sungai (meter)}

\item{n_days}{integer, jumlah hari pengamatan (default = 30)}

\item{rules}{data.frame berisi aturan spesies
(kolom wajib: species, elev_max, canopy_min, river_maxdist,
prob_camera, prob_daily)}
}
\value{
data.frame event kamera trap (kolom Grid, Image_Id, Photo_Type,
Genus_Species, Date_Time, Count)
}
\description{
Generate simulated camera trap events based on ecological rules
}
