% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gridStat.R
\name{gridStat}
\alias{gridStat}
\title{Calculate summary statistics for raster data by subgrids}
\usage{
gridStat(
  rasterData,
  subGrids,
  myFunc = mean,
  as_Raster = TRUE,
  rasRes = 1000,
  na.rm = TRUE
)
}
\arguments{
\item{rasterData}{A SpatRaster object (from \code{terra}), in UTM or projected coordinates.}

\item{subGrids}{A spatial grid (sf or SpatVector) defining subgrid polygons.
Each grid cell should have an ID column.}

\item{myFunc}{A function to apply (e.g., \code{mean}, \code{sum}, \code{max}, \code{sd}).}

\item{as_Raster}{Logical. If TRUE, returns raster output. If FALSE, returns polygon (sf).}

\item{rasRes}{Numeric. Output raster resolution (in map units, default = 1000).}

\item{na.rm}{Logical. Whether to ignore NA values when applying \code{myFunc}. Default TRUE.}
}
\value{
Either:
\itemize{
\item A raster (SpatRaster) of summary values per grid cell, if \code{as_Raster = TRUE}, or
\item A spatial polygon (sf) with an added \code{value} column, if \code{as_Raster = FALSE}.
}
}
\description{
This function calculates a summary statistic (e.g., mean, sum, max) of a raster
dataset for each subgrid polygon within a landscape. Optionally, the results
can be returned as a raster map.
}
\examples{
\dontrun{
gridStat(rasterData = dem, subGrids = grids, myFunc = mean, as_Raster = FALSE)
}

}
