% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clear_points.R
\name{clear_points}
\alias{clear_points}
\title{Thin spatial points based on minimum distance}
\usage{
clear_points(data, dist, crs = NULL, coords = c("X", "Y"))
}
\arguments{
\item{data}{An sf object or a data.frame containing coordinates.}

\item{dist}{Numeric. Minimum allowed distance between points (in CRS units, typically meters).}

\item{crs}{Optional. Coordinate reference system (EPSG code or proj4string).
Required if \code{data} is not an sf object.}

\item{coords}{Character vector of length 2 indicating coordinate column names
(default = c("X", "Y")).}
}
\value{
An sf object containing filtered points.
}
\description{
Removes points that are located within a specified minimum distance
of each other. Only one point is retained within each distance cluster.
}
\details{
The function uses spatial indexing via \code{sf::st_is_within_distance()}
to avoid constructing a full distance matrix.
The input CRS must be projected (e.g., UTM).
}
\examples{
library(sf)
pts <- data.frame(
  X = c(500000, 500010, 500500),
  Y = c(10000, 10005, 10500)
)
result <- clear_points(pts, dist = 50, crs = 32748)

}
